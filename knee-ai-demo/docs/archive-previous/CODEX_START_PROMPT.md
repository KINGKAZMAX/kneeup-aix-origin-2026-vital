请接手当前 AIR-FLOW-Knee+ 项目，直接在这个目录继续开发，不要另建前端，不要重做视觉。

先读 AGENTS.md、CODEX_HANDOFF.md、docs/AI_INTEGRATION_SPEC.md、README.md、HARDWARE_PROTOCOL.md 和 docs/HANDOFF_VERIFICATION.md，并核对源码。项目定位原文在 docs/reference/PROJECT_SOURCE_EXTRACT.md；参考图只是概念，不是已实现状态。

我们正在参加黑客松，此前说距离路演约 15 小时。当前已有 HTML/CSS/JS 前端及 USB Web Serial 只读接入，下一步要把真实模型的 AI 分析接到这个前端，而不是继续只画图或写方案。

本轮请按顺序落地：
1. 启动现有项目、跑基线测试，保留原版数字串口和 v1 遥测兼容、实时曲线、来源标记和 CSV/JSON。
2. 在 Live Assist 加入 AI 观察区：收到哪些数据、模型观察、建议、依据、局限、模型/模式、生成时间和调用状态。
3. 复用现有 device 数据流，由程序提取窗口摘要，经本机服务端调用配置的真实模型；优先手动“分析当前窗口”。密钥只留服务端；新增配置前先限制静态服务，不能暴露 .env 等私有文件。
4. 数据不足、无基线、模型未配置、失网、非法输出、重连或旧回复时明确处理。无 key 就完成适配器和测试并标明“真实调用未验证”；规则降级和模拟数据不能冒充 AI 或实测。外发前取得操作者授权。
5. 把输入窗口和 AI 结果写入会话记录，补测试、启动说明及路演验收步骤。

唯一确认的传感器是 A0 单路肌电；A5 是控制输出，不是压力或实际助力反馈。不得生成上楼/疲劳/坡度/助力百分比的实测结论。当前程序不是持续 500 Hz。这个阶段 AI 只观察和建议，不自动改阈值、不写串口、不控气泵；保留未来自适应主动助力的项目定位，但明确这次还不是 AI 物理闭环。

先简短报告代码现状和首批修改文件，然后直接做增量实现并测试。不要重复询问已有背景，不要只输出计划。最终分别报告代码完成、mock 测试、真实 API、真实 USB 的状态，给出启动命令和现场操作顺序。未获确认不要烧录固件、操作气动硬件、公开部署或购买服务。
