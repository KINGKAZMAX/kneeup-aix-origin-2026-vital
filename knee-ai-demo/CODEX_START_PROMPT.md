# 接手膝望当前路演交付版

状态日期：2026-09-05。当前目录为共享会话路演版，不要按历史交接文件重新实现已有模块。

先读本目录 `AGENTS.md`、`CODEX_HANDOFF.md`、`README.md`、`docs/SHARED_SESSION.md`、`HARDWARE_PROTOCOL.md`、`TEST_REPORT.md`，然后核对源码。归档 `docs/archive-previous/` 仅供追溯；旧通过计数、旧 UI 和“尚未接入 AI”等历史描述不代表当前版本。

用户最新确认的唯一配色规范是工作区 `项目二/design/UIUX配色规范-碳黑钴蓝-v1.md`，交付中附有“品牌规范”副本。所有界面、图表、PPT 与视频严格使用碳黑 × 钴蓝及配套 `tokens.css`；不再保留旧紫色视觉。

当前已实现：唯一只读 USB 实例、同机统一会话、独立本地摄像头／回放观察及左右侧、固定人工事件、手动 AI 窗口分析适配与证据／事件引用、同机专业复核和完整 JSON 导出。前端继续使用轻量 HTML/CSS/JavaScript；不要另建项目或迁移框架。

真实模型当前未配置，真实 API 未验收。系统枚举 Espressif `303a:1001` 仅是板卡线索，实际板型和板上固件未确认；本轮没有打开串口或烧录。压力、实际辅助力和气泵反馈仍未测得，网页与 AI 不发硬件命令。

接续工作应先检查最终测试输出与当前源码，然后补足真实模型、USB、摄像头及台架验收。窗口限最近 60 秒内本会话有效样本，至少 5 点／4 秒，最后样本与完整帧年龄 ≤7 秒；主动结束后须立即分析。不得混入结束后数据，不得将过期结果移入新会话。模型只发送经授权的肌电摘要和固定事件编号／类型／时间；摄像头角度、画面、自由文本和专业备注留在本机。

完整记录只在当前页面内存，刷新前导出；同机角色切换不代表跨设备或跨标签同步。保留合成／回放／规则解释与真实来源标签。未获得物理操作授权和现场条件时，不烧录、不操作气动硬件。不要自动公开部署、购买服务或寻找其他目录凭证。

启动 `python3 serve.py`，首页 `http://localhost:8000/`；直接采集入口 `http://localhost:8000/?view=hardware`。依当前测试报告运行必要验证，并分别汇报代码、软件替身测试、真实 API、真实 USB、摄像头及物理响应状态。
